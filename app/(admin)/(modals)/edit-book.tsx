import { Text, View } from "react-native";

const EditBookScreen = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Edit Book Modal</Text>
    </View>
  );
};

export default EditBookScreen;
