import { Text, View } from "react-native";

const CreateBookScreen = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Create Book Modal</Text>
    </View>
  );
};

export default CreateBookScreen;
